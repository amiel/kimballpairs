
<% categories.in_groups_of(3, false).each do |category_group| %>
  <div class="row">
    <% category_group.each do |category| %>
      <div class="span4">
        <div class="well">
          <h2><%= category.title %></h2>
          <h4><%= category.subtitle %></h4>
          <ul>
            <% category.kimball_pairs.each do |pair| %>
              <li><%= pair %></li>
            <% end %>
          </ul>
        </div>
      </div>
    <% end %>
  </div>
<% end %>
